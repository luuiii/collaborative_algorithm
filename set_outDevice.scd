(//using blackhole
o = Server.default.options;
o.outDevice = "fbstream";
s.reboot;
)

// (//set back to only speaker
// o = Server.default.options;
// o.outDevice = "Built-in Output";
// s.reboot;
// )